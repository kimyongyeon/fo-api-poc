CREATE TABLE users
(
    id    INT PRIMARY KEY,
    name  VARCHAR(50),
    email VARCHAR(50)
);

CREATE TABLE users_error
(
    id    INT AUTO_INCREMENT PRIMARY KEY,
    name  VARCHAR(50),
    email VARCHAR(50)
);